{
  "name": "archives-backend",
  "description": "WARC web-archive backend: a Bun HTTP API over a Postgres index, and the parser that fills it.",
  "module": "webserver.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run webserver.ts",
    "dev": "bun --hot webserver.ts",
    "parse": "bun run parser.ts",
    "parse:dev": "bun --hot parser.ts",
    "test": "bun test"
  },
  "dependencies": {
    "cli-progress": "^3.12.0",
    "@zip.js/zip.js": "^2.8.51",
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/cli-progress": "^3.11.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
